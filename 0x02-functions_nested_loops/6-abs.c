#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @t: the absolute value
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
